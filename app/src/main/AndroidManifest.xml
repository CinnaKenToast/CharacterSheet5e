<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shipsco.charactersheet">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name="com.shipsco.charactersheet.CharacterSheetApplication"
        android:allowBackup="true"
        android:icon="@mipmap/d20"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/d20_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CharacterSheet">
        <activity
            android:name="com.shipsco.charactersheet.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>